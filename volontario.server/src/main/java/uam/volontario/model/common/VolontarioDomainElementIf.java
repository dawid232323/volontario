package uam.volontario.model.common;

/**
 * Each Volontario-dedicated entity should implement this interface so all entities
 * can be accessed via single interface.
 */
public interface VolontarioDomainElementIf
{
    Long getId();
}
